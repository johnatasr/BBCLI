from app.cli import cli as start_cli


if __name__ == "__main__":
    """
    Entry point of the application.
    
    If executed as the main module:
    - The CLI is initiated by calling 'start_cli()' to interactively process commands.
    
    """
    start_cli()
